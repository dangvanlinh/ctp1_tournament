{
  "permissions": {
    "allow": [
      "mcp__Claude_Preview__preview_start",
      "Bash(where node)",
      "Bash(where npx)",
      "Bash(where serve)",
      "Bash(npx.cmd serve --version)",
      "Bash(node --version)",
      "Bash(npm install -g vercel)"
    ]
  }
}
